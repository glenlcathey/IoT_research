API being used - 
    shadow client:
        subscribes:
            $devices/ "name" /shadow/update
            $devices/ "name" /connected

        publishes:
            $devices/ "name" /shadow/update/accepted
            $devices/ "name" /shadow/update/delta

    device:
        subscribes:
            $devices/ "name" /shadow/update/accepted
            $devices/ "name" /shadow/update/delta       

        publishes:
            $devices/ "name" /shadow/update
            $devices/ "name" /connected


current to do :
    need to test the desired/delta portion
    build test bench gui to help debug in future/manage devices